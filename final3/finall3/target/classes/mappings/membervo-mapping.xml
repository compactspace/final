<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="MemberVO">
  
  <!-- 아오 이거 주석처리 login-mapping.xml로 다해라 그냥 주석으로 두자 -->
<!-- <insert id="insertmember" parameterType="userVO"  useGeneratedKeys="true" keyProperty="user_code" >
insert into user(user_where,id,password,naver_id,user_name) values(#{user_where},#{id},#{password},#{naver_id},#{user_name})

</insert>


<select id="checkidMembership" resultType="memberVO" >

select * from user where id=#{id}

</select>
		 -->
		
		
									


</mapper>